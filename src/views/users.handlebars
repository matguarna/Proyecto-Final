<div>
  {{#each users}}
    <li>
      <strong>Nombre: </strong>{{this.first_name}}
      -
      <strong>Apellido: </strong>{{this.last_name}}
      -
      <strong>Email: </strong>{{this.email}}
    </li>
  {{/each}}

  <center>
    {{#if hasPrevPage}}
      <a href="http://localhost:8080/api/usuarios/paginate?page={{prevPage}}"><button>Anterior</button></a>
    {{/if}}
    {{!-- <strong>Página {{page}}</strong> --}}
    {{#if hasNextPage}}
      <a href="http://localhost:8080/api/usuarios/paginate?page={{nextPage}}"><button>Siguiente</button></a>
    {{/if}}
  </center>
</div>